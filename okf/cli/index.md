# cli

The npx installer CLI. It copies the bundled slash command markdown files into a
Claude Code agent folder. The CLI is the only compiled TypeScript in the
repository; it does no autofixing itself.

- [install](/cli/install.md) — `issue_autofix install [agent_folder]`: copy the command files into `<agent_folder>/commands/`.
